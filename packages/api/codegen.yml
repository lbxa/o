overwrite: true
schema: "http://localhost:6969/graphql"
documents: null
generates:
  src/graphql.ts:
    plugins:
      - "typescript-common"
  ./graphql.schema.json:
    plugins:
      - "introspection"
